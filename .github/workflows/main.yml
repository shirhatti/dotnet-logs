name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      uses: azure/github-actions/dotnetcore-cli@master
      with:
        args: publish -r win-x64 /p:PublishSingleFile=true /p:PublishTrimmed=true

